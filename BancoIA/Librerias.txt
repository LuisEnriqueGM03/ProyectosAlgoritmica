pip install opencv-python

pip install ultralytics

pip install psycopg2

#Camara con CPU (LENTA)
pip install torch


#Camara con GPU (Rapida)
pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118